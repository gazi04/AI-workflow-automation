# Origin of the FastAPI backend, as reached from the browser.
# Must use the PUBLIC_ prefix so SvelteKit exposes it via $env/dynamic/public.
# Both REST calls and the workflow WebSocket derive from this value.
PUBLIC_API_URL=http://localhost:8000
